import { OpenApiCall } from "./analysis.openaimodel.js";
import { geminiApiCall } from "./analysis.geminimodel.js";

export async function anaylsisServices (userClaim) {

    //taking all the data generated by artificial intelligence
    const dataFromOpenAI = await OpenApiCall(userClaim)
    const dataFromGeminiAI = await geminiApiCall(userClaim)

    console.log("This is response from OpenAi : \n", dataFromOpenAI);
    console.log("\n\nThis is response from Gemini : \n", dataFromGeminiAI);
}